// ============================================================
// mockData.js — Only sidebar drafts remain here
// All blogs come from MongoDB via the Express API
// All users are authenticated against MongoDB
// ============================================================

// Sidebar "My Drafts" — local only (not yet persisted to DB)
export const mockDrafts = [];
